{#
/**
 * @file node--article--full.html.twig
 *
 * Full view mode for article nodes.
 */
#}

{# ── Section 1: Header ── #}

{% include 'saibher:articleBody' with {
  category: node.field_category.entity.name.value|default(''),
  title:    node.title.value,
  body:     node.body.value|default(''),
  image:    node.field_image.entity.uri.value|default(''),
} only %}